**۱**  
 **(ب)** اتمام زمان مجاز اجرای فرآیند

---

**۲  
 **(الف)** برنامه کاربر (کد برنامه)

---

**۳  
**(ب)** اطلاعاتی مربوط به وضعیت فرآیندها به حافظه منتقل می‌کنه

---

**۴**

- Mode Switch فقط بین حالت کاربر و کرنل اتفاق می‌افته (در همون فرآیند).
    
- Process Switch کل بستر اجرا (context) بین دو فرآیند عوض می‌شه.  
     پس **Process Switch** سربار بیشتری داره چون باید Context کامل ذخیره و بارگذاری بشه.
    

---

**۵**

- `جدید → آماده`: فرآیند تازه ساخته می‌شه.
    
- `آماده → اجرا`: CPU بهش داده می‌شه.
    
- `اجرا → منتظر`: منتظر ورودی یا I/O می‌مونه.
    
- `منتظر → آماده`: I/O تموم می‌شه.
    
- `اجرا → خاتمه`: کارش تموم می‌شه.
    
- `اجرا → آماده`: زمان CPU تموم می‌شه.
    
- `آماده → خاتمه`: سیستم می‌کشتش (kill).
    

---

**۶**  
بله، ممکنه. مثلاً یه فرآیند منتظر چند ورودی مختلف باشه (مثلاً از چند سوکت مختلف). برای کنترلش معمولاً از صف یا event flag استفاده می‌شه.

---

**۷**

- برنامه فقط یه فایل کده، مثلاً یه فایل exe.
    
- فرآیند وقتی به‌وجود میاد که اون برنامه **در حال اجرا** باشه.  
    یه برنامه می‌تونه چند فرآیند داشته باشه.
    

---

**۸**

```
   ┌──────────┐
   │  جدید    │
   └───┬──────┘
       │
       ▼
   ┌──────────┐
   │  آماده   │◄──────────────┐
   └───┬──────┘               │
       │                      │
       ▼                      │
   ┌──────────┐               │
   │   اجرا   │────────┐      │
   └───┬──────┘        │      │
       │                ▼      │
       ▼            ┌──────────┐
   ┌──────────┐     │  منتظر  │
   │ خاتمه    │     └──────────┘
   └──────────┘
```
